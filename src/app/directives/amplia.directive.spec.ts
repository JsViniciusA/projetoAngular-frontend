import { AmpliaDirective } from './amplia.directive';

describe('AmpliaDirective', () => {
  it('should create an instance', () => {
    const directive = new AmpliaDirective();
    expect(directive).toBeTruthy();
  });
});
