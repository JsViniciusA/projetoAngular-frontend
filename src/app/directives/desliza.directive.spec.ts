import { DeslizaDirective } from './desliza.directive';

describe('DeslizaDirective', () => {
  it('should create an instance', () => {
    const directive = new DeslizaDirective();
    expect(directive).toBeTruthy();
  });
});
